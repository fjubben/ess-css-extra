pipeline {
  options{skipDefaultCheckout()}
  agent{ label 'css'}
  stages {
    stage('test environment'){
      steps{
        sh 'echo ${branch}'
        sh 'echo ${pushArtifact}'
        sh "echo ${env.branch}"
        script {
          def myvar2 = 'helloworld'
        }
        sh 'echo ${myvar2}'
        sh 'export'
      }
    }
  }
}
